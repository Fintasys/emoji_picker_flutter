name: emoji_picker_flutter_example
description: Demonstrates how to use the emoji_picker_flutter plugin.
publish_to: "none"

environment:
  sdk: ">=2.17.0 <3.0.0"
  flutter: ">=3.0.0"

dependencies:
  flutter:
    sdk: flutter

  google_fonts: ^4.0.4
  emoji_picker_flutter:
    path: ../

dev_dependencies:
  flutter_lints: ^2.0.3
  integration_test:
    sdk: flutter

flutter:
  uses-material-design: true
